---
phase: 01-infrastructure-foundation
plan: 03
type: execute
wave: 3
depends_on: ['01-02']
files_modified: ['package.json', 'eslint.config.js']
autonomous: true
must_haves:
  truths:
    - 'ESLint checks both .js and .ts files'
    - 'npm run lint executes successfully'
  artifacts:
    - path: 'eslint.config.js'
      provides: 'Flat Config with TypeScript support'
  key_links: []
---

<objective>
Modernize the linting setup to use ESLint 9 Flat Config with full TypeScript support.

Purpose: Ensures consistent code quality across both JavaScript and TypeScript files.
Output: Updated linting configuration and package scripts.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-foundation/01-02-SUMMARY.md
@.planning/phases/01-infrastructure-foundation/01-RESEARCH.md
@eslint.config.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Linting Dependencies</name>
  <files>package.json</files>
  <action>Install `typescript-eslint` and update existing linting plugins to ensure compatibility with ESLint 9 Flat Config.</action>
  <verify>npm list typescript-eslint</verify>
  <done>Linting dependencies are installed.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor ESLint Configuration</name>
  <files>eslint.config.js</files>
  <action>
    Update `eslint.config.js` to:
    - Use `tseslint.config` helper.
    - Include `js.configs.recommended` and `tseslint.configs.recommended`.
    - Target `**/*.{js,jsx,ts,tsx}` in the `files` array.
    - Configure React Hooks and React Refresh plugins.
  </action>
  <verify>cat eslint.config.js</verify>
  <done>eslint.config.js follows the Flat Config pattern with TS support.</done>
</task>

<task type="auto">
  <name>Task 3: Update Package Scripts</name>
  <files>package.json</files>
  <action>
    Update the `lint` script in `package.json` to `eslint .`.
    Verify that the scripts `start` and `build` are correctly using `vite`.
  </action>
  <verify>npm run lint</verify>
  <done>Lint script is updated and passes (or reports valid errors).</done>
</task>

</tasks>

<verification>
1. Run `npm run lint`.
2. Verify that changing a .ts file to have a lint error is caught by the command.
</verification>

<success_criteria>
Linting system is modernized and covers the entire codebase (JS + TS).
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-foundation/01-03-SUMMARY.md`
</output>
