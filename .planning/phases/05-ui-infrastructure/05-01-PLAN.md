---
phase: 05-ui-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json, vite.config.ts, src/index.css]
autonomous: true
must_haves:
  truths:
    - "Tailwind CSS v4 is integrated via @tailwindcss/vite"
    - "Tailwind v4 base, components, and utilities layers are defined"
  artifacts:
    - path: "src/index.css"
      provides: "Tailwind v4 entry point with layer definition skeleton"
---

<objective>
Install and configure Tailwind CSS v4 as the primary styling engine. Establish the foundational CSS Cascade Layer order to prepare for hybrid styling with MUI.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui-infrastructure/05-CONTEXT.md
@.planning/phases/05-ui-infrastructure/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Tailwind v4 Dependencies</name>
  <files>package.json</files>
  <action>
    Install Tailwind v4 and its Vite plugin: `npm install tailwindcss @tailwindcss/vite`.
  </action>
  <verify>
    - `tailwindcss` and `@tailwindcss/vite` appear in `package.json`.
  </verify>
  <done>Tailwind v4 dependencies are installed.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite for Tailwind v4</name>
  <files>vite.config.ts</files>
  <action>
    Import `@tailwindcss/vite` and add it to the `plugins` array in `vite.config.ts`.
  </action>
  <verify>
    - `vite.config.ts` includes the tailwind vite plugin.
  </verify>
  <done>Vite is configured to process Tailwind v4.</done>
</task>

<task type="auto">
  <name>Task 3: Establish CSS Cascade Layers</name>
  <files>src/index.css</files>
  <action>
    1. Create `src/index.css` (or update if exists).
    2. Add `@import "tailwindcss";`.
    3. Define the layer order: `@layer mui, base, components, utilities;`. This ensures Tailwind utilities always have higher specificity than MUI components.
    4. Ensure `@layer mui {}` block exists for Emotion injection later.
  </action>
  <verify>
    - `src/index.css` contains the correct layer definitions and tailwind import.
  </verify>
  <done>CSS Cascade Layers are established.</done>
</task>

</tasks>

<verification>
1. Run `npm run dev` and ensure the project still builds.
2. Check that `src/index.css` is correctly loaded.
</verification>

<success_criteria>

- Tailwind v4 is integrated into the build pipeline.
- CSS Cascade Layer order is defined in the root stylesheet.
  </success_criteria>

<output>
After completion, create `.planning/phases/05-ui-infrastructure/05-01-SUMMARY.md`
</output>
