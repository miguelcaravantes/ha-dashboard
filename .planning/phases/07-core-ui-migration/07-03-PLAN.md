---
phase: 07-core-ui-migration
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  [
    "src/features/EntityCard/EntityCard.tsx",
    "src/features/EntityCard/PowerSwitch.tsx",
  ]
autonomous: true
must_haves:
  truths:
    - "Entity cards use the shadcn Card component for their container"
    - "PowerSwitch cards display a visible shadcn Switch component"
    - "Toggling the switch provides immediate optimistic UI feedback"
    - "Icons are prominent and colored based on entity state"
  artifacts:
    - path: "src/features/EntityCard/EntityCard.tsx"
      provides: "Refactored card container"
    - path: "src/features/EntityCard/PowerSwitch.tsx"
      provides: "shadcn-based PowerSwitch toggle"
  key_links:
    - from: "src/features/EntityCard/EntityCard.tsx"
      to: "src/components/ui/card.tsx"
      via: "Importing Card components"
    - from: "src/features/EntityCard/PowerSwitch.tsx"
      to: "src/components/ui/switch.tsx"
      via: "Importing Switch component"
---

<objective>
Refactor the core EntityCard container and the PowerSwitch component to use shadcn/ui.

Purpose: Migrates high-volume interactive components to the new design system with a focus on icon priority and visible controls.
Output: shadcn-based EntityCard and PowerSwitch.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/EntityCard/EntityCard.tsx
@src/features/EntityCard/PowerSwitch.tsx
@src/components/ui/card.tsx
@src/components/ui/switch.tsx
@.planning/phases/07-core-ui-migration/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor EntityCard container to shadcn</name>
  <files>src/features/EntityCard/EntityCard.tsx</files>
  <action>
    - Replace MUI Card and CardActionArea with shadcn `Card` component from `src/components/ui/card.tsx`.
    - Apply "Condensed" padding decision using Tailwind classes (e.g., `p-3` or `p-4`).
    - Implement "Icon Priority" by ensuring the entity icon is a focal point of the card layout.
    - Implement "Subtle Feedback" hover states using Tailwind (`hover:bg-accent` or border tints).
    - Remove MUI-specific styling and ensure the card integrates with Home Assistant native CSS variables for borders and backgrounds.
  </action>
  <verify>Check that all entity cards now use the shadcn Card styling.</verify>
  <done>EntityCard.tsx uses shadcn primitives and follows the new design decisions.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor PowerSwitch to shadcn Switch</name>
  <files>src/features/EntityCard/PowerSwitch.tsx</files>
  <action>
    - Replace MUI Switch with shadcn `Switch` component from `src/components/ui/switch.tsx`.
    - Ensure the "Visible Switch" decision is met — the toggle must be directly accessible on the card.
    - Implement "Optimistic Updates": Use local state or `useOptimistic` (if applicable) to toggle the UI immediately before the HA service call completes.
    - Maintain existing `useActionState` logic for the async toggle action.
    - Remove all MUI imports and styled components.
  </action>
  <verify>Toggle a PowerSwitch card and confirm it updates instantly and triggers the HA service.</verify>
  <done>PowerSwitch.tsx is fully migrated to shadcn/Tailwind with optimistic feedback.</done>
</task>

</tasks>

<verification>
Confirm that light/switch entities show the new shadcn card and switch, and that interactions feel fast due to optimistic updates.
</verification>

<success_criteria>
EntityCard and PowerSwitch are fully migrated and functional.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-ui-migration/07-03-SUMMARY.md`
</output>
