---
phase: 07-core-ui-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/features/Panel.tsx"]
autonomous: true
must_haves:
  truths:
    - "Dashboard has a fixed top header with navigation/actions"
    - "Mobile devices display a bottom navigation bar"
    - "The layout container is centered with a max-width"
  artifacts:
    - path: "src/features/Panel.tsx"
      provides: "Dashboard shell and navigation"
  key_links:
    - from: "src/features/Panel.tsx"
      to: "src/features/CardDashboard.tsx"
      via: "React component rendering"
---

<objective>
Migrate the primary dashboard shell and navigation from MUI to shadcn/ui and Tailwind v4.

Purpose: Establishes the new layout standards and navigation patterns required for the shadcn migration.
Output: Updated Panel.tsx with Top Header and Bottom Navigation.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/Panel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Panel shell to shadcn/Tailwind</name>
  <files>src/features/Panel.tsx</files>
  <action>
    - Replace MUI Container, AppBar, Toolbar, Tabs, and Tab with semantic HTML and Tailwind v4 classes.
    - Implement a fixed Top Header for primary actions (Menu, Title, Profile).
    - Implement a fixed Bottom Navigation for mobile devices using shadcn-like patterns (lucide icons).
    - Use "Centered Max-Width" layout for the main content area.
    - Ensure the 'handleMenuClick' logic for Home Assistant menu toggle is preserved.
    - Maintain existing TabPanel logic but style it with Tailwind.
    - Remove MUI-specific styled components (Container, Header, Title, Menu) from this file.
  </action>
  <verify>Run `npm run lint` and verify no MUI components remain in Panel.tsx except where strictly necessary for transition (none expected here).</verify>
  <done>Panel.tsx uses Tailwind v4 for layout and navigation shell, adhering to the Top Header + Bottom Nav decision.</done>
</task>

</tasks>

<verification>
Check that the dashboard shell renders with a fixed header and mobile-responsive bottom navigation.
</verification>

<success_criteria>
Dashboard shell is fully migrated to shadcn/Tailwind layout patterns.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-ui-migration/07-01-SUMMARY.md`
</output>
