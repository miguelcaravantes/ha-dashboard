---
phase: 07-core-ui-migration
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: ["src/features/CardDashboard.tsx"]
autonomous: true
must_haves:
  truths:
    - "The dashboard displays entities in a responsive grid without using MUI components"
    - "Grid columns adjust automatically based on screen width (auto-fill pattern)"
    - "Cards in the same row have equal height"
  artifacts:
    - path: "src/features/CardDashboard.tsx"
      provides: "Responsive dashboard grid"
  key_links:
    - from: "src/features/CardDashboard.tsx"
      to: "src/features/EntityCard/EntityCard.tsx"
      via: "Rendering entity cards"
---

<objective>
Replace the MUI-based Grid system in CardDashboard.tsx with a modern Tailwind v4 responsive grid.

Purpose: Optimizes information density and responsiveness using native CSS Grid via Tailwind.
Output: Updated CardDashboard.tsx with CSS Grid layout.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/features/CardDashboard.tsx
@.planning/phases/07-core-ui-migration/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate CardDashboard to Tailwind Grid</name>
  <files>src/features/CardDashboard.tsx</files>
  <action>
    - Remove all MUI Grid and Typography imports.
    - Replace the outer MUI Grid container with a `div` using Tailwind grid classes: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 auto-rows-fr`.
    - Apply the "Compact (Efficiency)" spacing decision by using reduced gaps (e.g., `gap-4`).
    - Use semantic `section` or `div` tags for room groupings instead of MUI Grid items.
    - Replace MUI Typography with standard `h2` or `h3` tags styled with Tailwind (e.g., `text-lg font-semibold mb-2`).
    - Ensure the room headers are properly integrated into the grid or maintain their hierarchy.
  </action>
  <verify>Check that the dashboard grid is responsive and cards have equal height in each row.</verify>
  <done>CardDashboard.tsx is free of MUI Grid components and uses Tailwind v4 grid utilities.</done>
</task>

</tasks>

<verification>
Verify the grid behavior by resizing the browser window. Ensure no layout breakage occurs.
</verification>

<success_criteria>
Dashboard uses a responsive, equal-height grid implemented with Tailwind v4.
</success_criteria>

<output>
After completion, create `.planning/phases/07-core-ui-migration/07-02-SUMMARY.md`
</output>
