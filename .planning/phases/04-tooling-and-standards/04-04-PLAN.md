---
phase: 04-tooling-and-standards
plan: 04
type: execute
wave: 4
depends_on: ['04-03']
files_modified:
  [
    src/features/EntityCard/EntityCard.tsx,
    src/features/ProfileImg.tsx,
    src/common/hassStore.ts,
    src/features/LightDetail/LightBrightness.tsx,
    src/features/LightDetail/LightGroup.tsx,
    src/features/Panel.tsx,
    package.json,
  ]
autonomous: true
must_haves:
  truths:
    - "Zero 'as' or '<Type>' assertions remain in the src/ directory (excluding 'as const')"
    - 'All components use type guards or robust typing for entity data'
    - 'The project build and lint pass successfully'
  artifacts:
    - path: 'src/common/hassStore.ts'
      provides: 'Type-safe store access'
    - path: 'src/features/Panel.tsx'
      provides: 'Safe window/panel integration'
  key_links:
    - from: 'src/features/Panel.tsx'
      to: 'window'
      via: 'logic check'
      pattern: 'typeof window'
---

<objective>
Perform a final sweep of all remaining type assertions across the codebase (DATA-04) and verify the hardened environment. This completes the standards enforcement phase.

Purpose: Achieve 100% compliance with strict assertion rules.
Output: A fully assertion-free codebase and successful final build.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tooling-and-standards/04-CONTEXT.md
@.planning/phases/04-tooling-and-standards/04-03-SUMMARY.md
@src/common/utils/typeGuards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remaining Assertions Sweep</name>
  <files>src/features/**/*.tsx, src/common/hassStore.ts, src/features/Panel.tsx</files>
  <action>
    1. Perform a sweep and refactor remaining violations in:
       - `src/features/EntityCard/EntityCard.tsx`
       - `src/features/ProfileImg.tsx`
       - `src/common/hassStore.ts`
       - `src/features/LightDetail/LightBrightness.tsx`
       - `src/features/LightDetail/LightGroup.tsx`
       - `src/features/Panel.tsx` (Handle window/panel integration safely per research)
    2. Replace all remaining `as` assertions with type guards or safe narrowing.
  </action>
  <verify>
    - `npm run lint` returns 0 errors across the entire `src/` directory.
  </verify>
  <done>All forbidden type assertions are removed from the codebase.</done>
</task>

<task type="auto">
  <name>Task 2: Final Verification and Hardening</name>
  <files>package.json</files>
  <action>
    1. Run a full project build: `npm run build`.
    2. Run a full project lint: `npm run lint`.
    3. Ensure no regressions in functionality (assertion-free code should handle missing data gracefully).
  </action>
  <verify>
    - Build succeeds.
    - Lint succeeds with zero errors.
  </verify>
  <done>The project is fully compliant with the new tooling and standards.</done>
</task>

</tasks>

<verification>
1. Run `grep -rE "\sas\s" src | grep -v "as const"` - should return 0 results.
2. Run `npm run lint` - should return 0 errors.
3. Run `npm run build` - should succeed.
</verification>

<success_criteria>

- Zero forbidden type assertions remain in the `src/` directory.
- Build and lint pass without any issues.
- All components use safe type-narrowing patterns.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-tooling-and-standards/04-04-SUMMARY.md`
</output>
