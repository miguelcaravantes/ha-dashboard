---
phase: 04-tooling-and-standards
plan: 03
type: execute
wave: 3
depends_on: ['04-02']
files_modified:
  [
    src/features/LightDetail/useLightDetail.ts,
    src/features/FanDetail.tsx,
    src/features/LightDetail/LightColor.tsx,
  ]
autonomous: true
must_haves:
  truths:
    - 'High-violation features are assertion-free'
    - 'Light and Fan components use type guards for attribute access'
  artifacts:
    - path: 'src/features/LightDetail/useLightDetail.ts'
      provides: 'Safe light attribute access'
    - path: 'src/features/FanDetail.tsx'
      provides: 'Safe fan attribute access'
  key_links:
    - from: 'src/features/LightDetail/useLightDetail.ts'
      to: 'src/common/utils/typeGuards.ts'
      via: 'import'
      pattern: 'isString|isNumber'
---

<objective>
Refactor high-violation features identified in the DATA-04 audit. This focuses on files with multiple unsafe assertions to maintain a manageable scope per plan.

Purpose: Eliminate the densest areas of unsafe code.
Output: Assertion-free high-priority feature components.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tooling-and-standards/04-CONTEXT.md
@.planning/phases/04-tooling-and-standards/04-02-SUMMARY.md
@src/common/utils/typeGuards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor High-Violation Features</name>
  <files>src/features/LightDetail/useLightDetail.ts, src/features/FanDetail.tsx, src/features/LightDetail/LightColor.tsx</files>
  <action>
    1. Refactor `src/features/LightDetail/useLightDetail.ts` (remove `as number[]`, `as number`).
    2. Refactor `src/features/FanDetail.tsx` (remove attribute assertions).
    3. Refactor `src/features/LightDetail/LightColor.tsx` (remove `as unknown as number[]`).
    4. Replace assertions with guards from `src/common/utils/typeGuards.ts` or inline narrowing.
    5. Prefer "Silent Fail / Early Return" pattern if narrowing fails for critical HA data.
  </action>
  <verify>
    - These files pass lint with zero assertion errors.
    - `tsc` confirms valid types.
  </verify>
  <done>The features with the highest density of assertions are refactored.</done>
</task>

</tasks>

<verification>
1. Run `npm run lint` specifically on the modified feature files.
2. Confirm no regressions in the Detail dialogs for lights and fans.
</verification>

<success_criteria>

- High-violation feature files contain zero forbidden assertions.
- Build and lint pass for these specific files.
- All attribute access in these components is type-safe.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-tooling-and-standards/04-03-SUMMARY.md`
</output>
