---
phase: 04-tooling-and-standards
plan: 03
type: execute
wave: 3
depends_on: ['04-02']
files_modified:
  [src/features/**/*.tsx, src/features/**/*.ts, src/common/**/*.ts]
autonomous: true
must_haves:
  truths:
    - "Zero 'as' or '<Type>' assertions remain in the src/ directory (excluding 'as const')"
    - 'All feature components use type guards or robust typing for entity data'
    - 'The build passes with strict assertion rules enabled'
  artifacts:
    - path: 'src/features/LightDetail/useLightDetail.ts'
      provides: 'Safe light attribute access'
    - path: 'src/features/FanDetail.tsx'
      provides: 'Safe fan attribute access'
  key_links:
    - from: 'src/features/LightDetail/useLightDetail.ts'
      to: 'src/common/utils/typeGuards.ts'
      via: 'import'
      pattern: 'isString|isNumber'
---

<objective>
Refactor all remaining type assertions across the codebase (DATA-04). This completes the transition to a fully type-safe environment where types are narrowed through logic rather than forced through casting.

Purpose: Eliminate the remaining unsafe code paths in the feature layer.
Output: An assertion-free codebase.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-tooling-and-standards/04-CONTEXT.md
@.planning/phases/04-tooling-and-standards/04-02-SUMMARY.md
@src/common/utils/typeGuards.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor High-Violation Features</name>
  <files>src/features/LightDetail/useLightDetail.ts, src/features/FanDetail.tsx, src/features/LightDetail/LightColor.tsx</files>
  <action>
    1. Refactor `src/features/LightDetail/useLightDetail.ts` (multiple `as number[]`, `as number` assertions).
    2. Refactor `src/features/FanDetail.tsx` (multiple attribute assertions).
    3. Refactor `src/features/LightDetail/LightColor.tsx` (`as unknown as number[]` pattern).
    4. Replace assertions with type guards from `src/common/utils/typeGuards.ts` or inline narrowing.
    5. Prefer "Silent Fail / Early Return" pattern if narrowing fails for critical HA data.
  </action>
  <verify>
    - These files pass lint with zero assertion errors.
    - `tsc` confirms valid types.
  </verify>
  <done>The features with the highest density of assertions are refactored.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Remaining Assertions (DATA-04 Sweep)</name>
  <files>src/features/**/*.tsx, src/features/**/*.ts, src/common/hassStore.ts, src/features/ProfileImg.tsx</files>
  <action>
    1. Perform a sweep of the remaining files identified with assertions:
       - `src/features/EntityCard/EntityCard.tsx`
       - `src/features/ProfileImg.tsx`
       - `src/common/hassStore.ts`
       - `src/features/LightDetail/LightBrightness.tsx`
       - `src/features/LightDetail/LightGroup.tsx`
       - `src/features/Panel.tsx` (handle window casting carefully per research recommendations)
    2. Refactor each violation to use type guards or safe logic.
    3. Note: Research allows window casting for panel integration if safe, but try to abstract it if possible.
  </action>
  <verify>
    - `npm run lint` returns 0 errors across the entire `src/` directory.
  </verify>
  <done>All forbidden type assertions are removed from the codebase.</done>
</task>

<task type="auto">
  <name>Task 3: Final Verification and Hardening</name>
  <files>package.json</files>
  <action>
    1. Run a full project build: `npm run build`.
    2. Run a full project lint: `npm run lint`.
    3. Ensure no regressions in functionality (assertion-free code should handle missing data gracefully).
  </action>
  <verify>
    - Build succeeds.
    - Lint succeeds with zero errors.
  </verify>
  <done>The project is fully compliant with the new tooling and standards.</done>
</task>

</tasks>

<verification>
1. Run `grep -rE "\sas\s" src | grep -v "as const"` - should return 0 results.
2. Run `npm run lint` - should return 0 errors.
3. Run `npm run build` - should succeed.
</verification>

<success_criteria>

- Zero forbidden type assertions remain in the `src/` directory.
- Build and lint pass without any issues.
- All feature components use safe type-narrowing patterns.
  </success_criteria>

<output>
After completion, create `.planning/phases/04-tooling-and-standards/04-03-SUMMARY.md`
</output>
